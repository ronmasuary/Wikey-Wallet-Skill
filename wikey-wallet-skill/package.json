{
  "name": "wikey-wallet-skill",
  "version": "2.2.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsc --noEmit && node --test --import tsx 'src/*.test.ts'"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22",
    "tsx": "^4"
  }
}
